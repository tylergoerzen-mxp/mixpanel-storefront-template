# Don't auto-run `pnpm install` before scripts like `pnpm build`/`pnpm dev`.
# This keeps `next build` from re-triggering install (and tripping on build
# script approvals) in CI and on Vercel.
verify-deps-before-run=false
